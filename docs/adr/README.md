# Architecture decision records

Records are append-only. If a decision changes, add a new record that supersedes the earlier one rather than rewriting history.

Status values are `Proposed`, `Accepted`, `Superseded` and `Rejected`.
