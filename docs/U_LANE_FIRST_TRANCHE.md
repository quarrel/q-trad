# U-lane first tranche

Status: **ACCEPTED — source feasibility and fixture implementation only**, following independent review. Outcome class: **DATA_LIMITED**; no empirical selection-value result.

## Authority and delivered scope

The operator approved `U_LANE_IMPLEMENTATION_PLAN.md` on 2026-09-09. Activation commit `922da41` records that approval against working base `cff6cd272b5605d9c64fed05fe75f9ad217638c1`. The independently approved source and laboratory candidate is `61c02c2b82384ae23b55ba489b5507634495397a`, following repairs to initial candidate `2322f2c`. Delivery is a local committed handoff on `u-lane/first-tranche`; no push or merge was performed.

Empirical inputs and development ranges are **NONE_APPROVED**. No provider account, market-data acquisition, purchase, collector operation, empirical payload or reserve outcome was accessed for this tranche. Source feasibility uses public documentation; all numerical laboratory inputs are invented fixtures. Work remains `IMPLEMENTATION_ONLY`. No U3 acquisition, U4 reserve evaluation, promotion or R5/trading activation follows.

## Source feasibility and missing decision

[U0 source readiness](U0_SOURCE_READINESS.md) compares three candidates and records a twelve-family proposed shortlist, with unresolved mapping, economics, field availability, licence and historical coverage explicitly retained. Databento GLBX.MDP3 is the provisional metadata/quote route; no provider or dataset is empirically qualified. Its documented UTC daily aggregation cannot silently be relabelled an exchange session. CME remains a backup; CHRIS could not be qualified through available public pages.

The next decision is the report's bounded metadata/quote-only envelope: approved account context and destination, no outcome values, proposed cumulative limits of 20 requests, 5 MB metadata and USD 0 charges. These caps are recommendations awaiting approval. A numerical quote, exact products/dates/fields/licence/retention and separate acquisition limits must precede any purchase or retrieval. Unavailable price information is not a zero-price estimate.

## Split and exposure

The actual unrestricted research environment does not establish reserve isolation. The fixture loader rejects reserve requests before Parquet decoding, but this does not prove filesystem, shell, connector or provider exclusion. Before empirical work, expose only approved development extracts and leave the reserve unacquired or effectively inaccessible to all research roles. A mixed archive requires separately authorised external splitting; no operator evidence was moved or deleted here.

Freeze the empirical endpoint using non-value availability metadata and reserve the final eight completed quarters. If the endpoint is 2026-Q2, the reserve is 2024-Q3–2026-Q2; this is an example, not an approved input interval. Prior R2/R4 exposure to parts of 2026 remains disclosed. No claim of a globally untouched period or original R2 period comparison is made.

## Fixture laboratory

[The runnable laboratory](../experiments/universe_discovery/README.md) provides a synthetic Parquet panel, ordinary specification written before scoring, saved selection register, physical position ledgers and append-only run register. Quarterly cohorts use preceding twelve-calendar-month descriptors; sixteen-week snapshots are diagnostic. Eligibility, score formulas, missingness, group/redundancy limits, seeds, schedules and costs are frozen in each run specification.

The two named H-DAY probes are continuation and reversal. Prices, monetary changes and physical costs use `Decimal`; lagged risk scaling and chronological positions avoid overlapping full-sized trades. Synthetic Friday, quarter-end and catalogue last-tradable dates constrain exits. These are explicit fixture semantics, not inferred real exchange delivery rules.

Controls comprise eligible synthetic original anchors, size-matched anchor views, range/cost selection, stratified random draws and nuisance-matched random draws. One hundred fixed draws retain distinct matching assignments and legitimate cohort overlap. Missing matching or required outcomes stay unavailable. Per-market outcomes are calculated once and reused by cohorts and cost scenarios.

Primary comparisons use equal-member and equal-vintage aggregation, with signed matched contributions reconciled before positive concentration clipping. Tail, support and cost-scenario diagnostics accompany the reductions. Synthetic scorable comparisons demonstrate software behaviour; they do not establish market opportunity, selection value or advancement.

## Validation and measured scale

Owner evidence for the reviewed candidate:

| Check | Result |
| --- | --- |
| Focused fixture tests | 23 passed in 10.82 s; four selector cases rechecked after final test repair |
| Namespace Ruff lint and formatting | Passed |
| Namespace strict Pyright | Passed |
| Source claims and permissions | Documentation checks passed; no empirical readiness implied |
| Staged exact patch whitespace | Passed |
| Independent combined review | PASS at `61c02c2`; both findings closed; unchanged U0/reducer conclusions carried forward |

The representative `fixture-003` run uses 18,816 rows, 24 synthetic families, eight quarters, two probes and 100 draws. The owner measured 6.39 s CPU, 197.5 MiB peak RSS and 14.58 MB of outputs. Work counts were one panel decode, 24 market preparations, 384 market/vintage/probe outcome calculations and zero model fits. Both probes under base and doubled variable friction were scorable across eight fixture quarters. This is representative fixture performance, not a measured twenty-year global-universe forecast.

The application, shared dependencies and schema are unchanged; focused namespace checks are proportionate. Independent review found and closed unavailable-entry-input handling and missing selector-path tests. Session reasons and separate payoff coverage now prevent unavailable risk/signals becoming successful flat zero returns. Four deterministic no-edge/planted cases traverse descriptors, selection, all declared control draws, shared scoring and reduction for both probes. Their one-session forward support tests plumbing, not statistical false-positive calibration. Concentration reconciliation, unequal cohort sizes, signed cancellation, missing support, matching failure and causal/ledger fixtures also pass. Test commands are in the laboratory README.

## Limitations, retained outputs and next action

No real daily atlas, latest empirical sixteen-week comparison, original R2 interval comparison, empirical confidence claim or U3 acquisition recommendation has been scored. Empirical calendars, historical membership, field publication, product economics, rolls, FX/carry and execution costs require source qualification. Daily bars cannot establish delayed-close fills, intraday stops, depth, capacity or session/event attribution.

Synthetic attempts and their append-only register remain under `tmp/agents/u-lane-20260909/u12/fixtures/` for review and reproducibility; `fixture-001` and `fixture-002` are earlier attempts, while `fixture-003` is the accepted candidate run. They are implementation evidence, not retained research evidence. Ignored programme state in `tmp/agents/u-lane-20260909/` supports recovery and the execution audit. These named outputs remain useful for the requested post-mortem; no broad temporary-file cleanup was performed. The tracked [MAP observations](U_LANE_MAP_POST_MORTEM.md) record problems and efficiency findings.

Next: the operator can approve or revise the concrete metadata/quote envelope in U0 source readiness. Until then, empirical inputs remain `NONE_APPROVED`. R4 conclusions, held-out limitations and the R5 boundary remain unchanged.
